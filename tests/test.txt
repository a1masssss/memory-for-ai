curl -s "http://localhost:8080/health"

curl -s -X POST "http://localhost:8080/turns" \
  -H "Authorization: Bearer almas-token" \
  -H "Content-Type: application/json" \
  -d '{
    "session_id": "manual-2",
    "user_id": "user-manual-2",
    "messages": [
      {
        "role": "user",
        "content": "make video mode cinematic, and I love when picture is more realistic and do not make face plastic"
      },
      {
        "role": "assistant",
        "content": "Got it. I will keep that in mind."
      }
    ],
    "timestamp": "2025-03-15T10:30:00Z",
    "metadata": {
      "source": "manual-test"
    }
  }'

curl -s -X POST "http://localhost:8080/recall" \
  -H "Authorization: Bearer almas-token" \
  -H "Content-Type: application/json" \
  -d '{
    "query": "what more prefers user?",
    "session_id": "manual-2",
    "user_id": "user-manual-2",
    "max_tokens": 512
  }'

curl -s \
  -H "Authorization: Bearer almas-token" \
  "http://localhost:8080/users/user-manual-2/memories"

curl -s \
  -H "Authorization: Bearer almas-token" \
  "http://localhost:8080/users/user-manual-2/memories"

curl -i -X DELETE \
  -H "Authorization: Bearer almas-token" \
  "http://localhost:8080/users/user-manual"
