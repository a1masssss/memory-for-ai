APP_NAME=memory-service
DATABASE_URL=postgresql://memory:memory@postgres:5432/memory

# Optional. If set later, routes can enforce Authorization: Bearer <token>.
MEMORY_AUTH_TOKEN=

POSTGRES_DB=memory
POSTGRES_USER=memory
POSTGRES_PASSWORD=memory
